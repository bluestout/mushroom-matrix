<anchor-element id="{{ section.settings.anchor }}">
</anchor-element>

<script src="{{ 'anchor-element.js' | asset_url }}" defer="defer"></script>

{% schema %}
{
  "name": "Anchor",
  "class": "section",
  "disabled_on": {
    "groups": ["header", "footer"]
  },
  "settings": [
    {
      "type": "text",
      "id": "anchor",
      "label": "Anchor"
    }
  ],
  "presets": [
    {
      "name": "Anchor"
    }
  ]
}
{% endschema %}
